# ========================================
# 환경변수 설정 예시
# ========================================
# 
# 사용법:
# 1. 이 파일을 복사하여 .env, .env.development, .env.production 생성
# 2. 각 환경에 맞는 값으로 수정
# 
# 주의: 
# - 환경변수는 빌드 시에만 읽힘 (정적 배포 특성)
# - NEXT_PUBLIC_ 접두사 = 클라이언트 노출 가능
# - 접두사 없음 = 서버 사이드 전용 (SSG 빌드 시)
# ========================================

# ----------------------------------------
# 필수 환경변수 (Required)
# ----------------------------------------

# API 기본 URL
NEXT_PUBLIC_API_URL=https://jsonplaceholder.typicode.com

# API 타임아웃 (밀리초)
NEXT_PUBLIC_API_TIMEOUT=30000

# ----------------------------------------
# 선택 환경변수 (Optional)
# ----------------------------------------

# API Accept-Language 헤더
NEXT_PUBLIC_API_ACCEPT_LANGUAGE=ko-KR

# 디버그 모드 (개발 시 상세 로그 출력)
NEXT_PUBLIC_FEATURE_DEBUG=false

# ----------------------------------------
# 개발 환경 전용 (Development Only)
# ----------------------------------------

# API 프록시 타겟 (CORS 우회용, 프로덕션 무시됨)
# 예: http://localhost:3001
# API_TARGET_URL=

# 번들 분석 활성화
# ANALYZE=false
