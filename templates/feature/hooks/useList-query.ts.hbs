'use client';

import type { {{pascalCase name}} } from '@/entities/{{camelCase name}}';
import { useQuery } from '@tanstack/react-query';
import { get{{pascalCase name}}s } from '../api';

export function use{{pascalCase name}}sQuery() {
  return useQuery<{{pascalCase name}}[]>({
    queryKey: ['{{camelCase name}}s'],
    queryFn: get{{pascalCase name}}s,
  });
}

