'use client';

import { use{{pascalCase name}}s } from '../hooks';
import styles from './{{pascalCase name}}List.module.css';

export function {{pascalCase name}}List() {
  const { {{camelCase name}}s, isLoading, error } = use{{pascalCase name}}s();

  return (
    <div className={styles.container}>
      <h1>{{pascalCase name}} 목록</h1>

      {isLoading && <div className={styles.loading}>로딩 중...</div>}

      {error && <div className={styles.error}>에러: {error.message}</div>}

      {!isLoading && {{camelCase name}}s.length > 0 ? (
        <div className={styles.grid}>
          {{{camelCase name}}s.map(item => (
            <div key={item.id} className={styles.card}>
              <div className={styles.id}>{item.id}</div>
              {/* TODO: 항목 내용 표시 */}
            </div>
          ))}
        </div>
      ) : !isLoading ? (
        <p className={styles.empty}>데이터가 없습니다.</p>
      ) : null}
    </div>
  );
}

