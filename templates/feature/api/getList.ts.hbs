import type { {{pascalCase name}} } from '@/entities/{{camelCase name}}';
import { fetchAPI } from '@/shared/api';
import { logger } from '@/shared/lib';

export async function get{{pascalCase name}}s(): Promise<{{pascalCase name}}[]> {
  try {
    const data = await fetchAPI<{{pascalCase name}}[]>('{{endpoint}}');
    return data;
  } catch (error) {
    logger.error('get{{pascalCase name}}s 에러:', error);
    throw error;
  }
}

